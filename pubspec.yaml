name: flutter_wilayah_indonesia
description: "A Flutter package that provides complete offline data of Indonesian administrative regions (Province, City/District, Subdistrict, Village) and ready-to-use cascading dropdown widgets for seamless form integration."
version: 0.0.1
homepage:

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
assets:
  - lib/assets/provinsi.json
